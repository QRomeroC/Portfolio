
public class SortGrid {
	public static void sortA(Grid g) {
		int col = 0;
		for (int row = 0; row <= g.size(); row++) {
			heapRow(g.getLoc(row,col),col);
			col++;
		}
	}
	
	private static void heapRow(Loc currLoc, int col) {
		for (int i = 0; i < ; i++) {
			
		}
	}
	public static void sortB(Grid g) {
		
	}
}
